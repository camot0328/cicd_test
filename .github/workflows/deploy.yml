name: github actions
on:
  push:
    branches: [main]

jobs:
  My-First-CICD-job:
    runs-on: ubuntu-latest

    steps:
      - name: simple-web
        run: echo "This is the first auto cicd step 1"
